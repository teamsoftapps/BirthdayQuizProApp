import {StyleSheet, Text, View} from 'react-native';
import React from 'react';
import {colors} from '../../../utlies';

const All = () => {
  return (
    <View style={{}}>
      <Text style={{color: '#000'}}>All</Text>
    </View>
  );
};

export default All;

const styles = StyleSheet.create({});
